apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodejsapp.fullname" . }}-test-connection"
  labels:
    {{- include "nodejsapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodejsapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
