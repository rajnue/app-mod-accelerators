import { ValidateUserNameDirective } from './validate-user-name.directive';
import { CustomvalidationService } from '../../core/validations/password-validation';

/**
describe('ValidateUserNameDirective', () => {
  customValidator: CustomvalidationService;
  it('should create an instance', () => {
    const directive = new ValidateUserNameDirective(this.customValidator);
    expect(directive).toBeTruthy();
  });
});
*/
