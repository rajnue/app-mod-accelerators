import { PasswordPatternDirective } from './password-pattern.directive';
import { CustomvalidationService } from '../../core/validations/password-validation';
/**
describe('PasswordPatternDirective', () => {
  customValidator: CustomvalidationService;

  it('should create an instance', () => {
    const directive = new PasswordPatternDirective(this.customValidator);
    expect(directive).toBeTruthy();
  });
});
 */
